# Modules — Crates & Visibility

`mod`, `pub`, `use`, re-exports, visibility rules

**6 topics | Progressive difficulty**

```bash
cargo test -p modules_intro
```

## Topics

1. **Module Basics** — `mod`, inline modules, `pub`, private by default
2. **Nested Modules** — Module hierarchy, `super`, path resolution
3. **`use` and Aliases** — `use`, `as`, glob imports
4. **Visibility Rules** — `pub(crate)`, `pub(super)`, struct field visibility
5. **Re-exports** — `pub use`, creating a clean public API
6. **Advanced — Plugin Architecture** — Modules + traits for extensible systems
