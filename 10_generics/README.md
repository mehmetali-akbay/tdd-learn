# Generics — Generic Types & Functions

Type parameters, constraints, generic structs

**35 functions | 31 tests**

```bash
cargo test -p generics
```

## Topics

1. **Generic Functions** — <T>, T: PartialOrd, T: Clone
2. **Wrapper<T> — Generic Struct** — Structs with type parameters
3. **Pair<T, U> — Multiple Type Parameters** — Multiple generic params, different types
4. **Stack<T> — Generic Collection** — Building a collection with generics
5. **Generic Constraints** — Multiple bounds: PartialOrd + Copy, where clauses
6. **Advanced — Fn Bounds & Pipeline** — Generic functions as parameters, closures
