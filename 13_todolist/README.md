# Todo List — Structs & Data Management

CRUD, filtering, sorting, serialization, IDs

**33 functions | 34 tests**

```bash
cargo test -p todolist
```

## Topics

1. **TodoItem — Struct Design** — Struct with multiple fields, Display, equality
2. **TodoList — Collection Management** — Vec-backed collection, CRUD, ID generation
3. **Filtering** — Iterator-based filtering, closures
4. **Sorting** — sort_by, Ord, multi-key sorting
5. **Serialization — to/from string** — Custom serialization (simple text format)
6. **Advanced — Statistics & Bulk Operations** — Aggregation, batch operations
