# Files — File I/O & String Processing

Reading/writing strings as "files", parsing, CSV, config

**30 functions | 27 tests**

```bash
cargo test -p files
```

## Topics

1. **Line Processing** — split lines, trim, filter empty, enumerate
2. **CSV Parsing** — Splitting, parsing structured data
3. **Config File Parsing** — Key=value parsing, comments, sections
4. **Text Statistics** — Counting, frequency analysis
5. **Text Transformations** — Line-by-line transforms, sorting, reversing
6. **Advanced — Log Parsing** — Complex multi-field parsing
