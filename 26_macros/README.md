# Macros — macro_rules!

Declarative macros, patterns, repetitions, TT munching

**6 functions | 21 tests**

```bash
cargo test -p macros
```

## Topics

1. **Basic Macro Patterns** — Simple matching, expression macros
2. **Repetition Patterns** — *, +, ?, nested repetitions
3. **Pattern Matching in Macros** — Multiple macro arms, different input patterns
4. **Struct & Enum Generation** — Using macros to generate types and impls
5. **Utility Macros** — Debug helpers, timing, error handling
6. **Advanced — Recursive Macros** — TT munching, recursive expansion
